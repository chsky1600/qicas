# MongoDB connection string (replace with production credentials)
MONGO_URI=mongodb://localhost:27017/qicas

# Server port
PORT=3000

# JWT secret (replace with a strong random string in production)
JWT_SECRET=CHANGE_ME
